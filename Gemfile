source 'https://rubygems.org'

gem 'wavefile'
gem 'bezier_curve'

group :development do
  gem 'pry-byebug' # 'nicer irb' and a debugger glued together; start with 'binding.pry'

  gem 'guard'
  gem 'guard-shell'
end
