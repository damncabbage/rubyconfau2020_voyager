source 'https://rubygems.org'

gem 'wavefile'

# For line-straightening
gem 'bezier_curve'

# For "just show me some numbers"
gem 'unicode_plot'

group :development do
  gem 'pry-byebug' # 'nicer irb' and a debugger glued together; start with 'binding.pry'

  gem 'guard'
  gem 'guard-shell'
end
